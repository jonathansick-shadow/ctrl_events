# -*- python -*-
Import('env')

env.SharedLibrary('events', map(lambda str: "../src/events/" + str,
                            ["Events.cc",
                             "EventFormatter.cc",
                             "EventLog.cc",
                             "EventSystem.cc",
                             ]), LIBS=env.libs["boost"]+env.libs["pex_data"]+env.libs["pex_policy"]+env.libs["pex_exceptions"]+env.libs["pex_logging"]+env.libs["activemqcpp"]+env.libs["python"])
