setupRequired(boost >= 1.47.0)
setupRequired(activemqcpp >= 3.1.2)
setupRequired(utils >= 4.7.3.0)
setupRequired(daf_base >= 4.7.3.0)   
setupRequired(pex_logging >= 4.7.3.0)
setupRequired(pex_exceptions >= 4.7.3.0)  
setupRequired(pex_policy >= 4.7.3.0)
setupRequired(cat >= 3.3)
setupRequired(daf_persistence >= 4.7.3.0)

setupOptional("swig")
setupOptional("doxygen")

envAppend(LD_LIBRARY_PATH, ${PRODUCT_DIR}/lib)
envAppend(DYLD_LIBRARY_PATH, ${PRODUCT_DIR}/lib)

envAppend(PYTHONPATH, ${PRODUCT_DIR}/python)
envAppend(PATH, ${PRODUCT_DIR}/bin)

