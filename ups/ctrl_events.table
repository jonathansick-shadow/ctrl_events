setupRequired(boost >= 1.37.0)   
setupRequired(activemqcpp >= 2.2.6)
setupRequired(utils >= 3.4.2)
setupRequired(daf_base >= 3.2.3)   
setupRequired(pex_logging >= 3.3.2)
setupRequired(pex_exceptions >= 3.2.2)  
setupRequired(pex_policy >= 3.3.1)

setupOptional("swig")
setupOptional("doxygen")

envAppend(LD_LIBRARY_PATH, ${PRODUCT_DIR}/lib)
envAppend(DYLD_LIBRARY_PATH, ${PRODUCT_DIR}/lib)

envAppend(PYTHONPATH, ${PRODUCT_DIR}/python)
envAppend(PATH, ${PRODUCT_DIR}/bin)

